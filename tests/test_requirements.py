# when you run pip install -r requirements.txt, pip will install these packages at the specified versions. This ensures that your testing environment is consistent and reproducible, which helps prevent issues caused by differences in package versions.
pytest==8.0.0,
requests==2.25.1